import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.gustavocartestaller1',
  appName: 'gustavocartestaller1',
  webDir: 'www'
};

export default config;
